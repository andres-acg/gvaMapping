After knitting and rendering `gvaMapping.Rmd`, overwrite this file with a symlink to `gvaMapping.md`.

E.g., on Linux/macOS from terminal:

```bash
cd C:/Users/ANCAG6/OneDrive - Université Laval/LICHEN_project/paper1/SpaDES_version/gvaMapping
rm README.md && ln -s gvaMapping.md README.md
```
